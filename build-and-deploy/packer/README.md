# Golang application with packer packaged as an AMI to a VM
This blueprint shows an end-to-end workflow for a deployment of a Golang application using:
* Packer to build and package the application as an AMI
* Github actions for the CI pipeline

# Next Steps
You can combine this blueprint with the ASG blueprint for deployment and asg-update-image deploy workflow for full automatic delivery of new versions
